# VoiceFlow 产品需求文档 (PRD)

---

## 1. 产品概述与愿景

### 1.1 产品定位
`VoiceFlow` 是一款专为 macOS 设计的**本地离线、高精度语音输入与听写增强工具**。它致力于通过全本地的自动语音识别（ASR）引擎，在完全保障用户隐私的前提下，为用户提供极其顺畅、即时、高精度且无缝的日常文本录入体验。

### 1.2 核心痛点
- **在线听写隐私泄露**：市面上大部分高精度输入法依赖云端 API，存在商业或隐私数据泄露风险。
- **系统听写灵活性差**：macOS 原生语音输入交互不够灵活，难以定制离线模型或在各软件间实现高度自定义的注入。
- **非流式模型无实时预览**：离线高精度模型（如 SenseVoice）是非流式的，录音中途悬浮窗空白，用户缺乏即时打字确认的安全感。
- **中英混合识别精度与纠错能力有限**：开源本地 ASR 对某些特定专有名词（如开发工具或专业术语）识别准确度不够，且口语化输入中包含大量废话语气词。

### 1.3 解决思路
全本地加载 ASR 引擎（如 Paraformer, SenseVoice），并通过**双引擎并行流式架构**（Apple Native ASR 实时流式预览 + SenseVoice 离线高精度覆盖），在实现 100% 离线隐私保护的同时，达成“录音期间字随音出，录音结束高精覆盖”的极致产品体验。在应用层提供词典纠偏和语气词强力过滤，弥补底层模型的限制。

---

## 2. 核心功能需求 (Features)

### 2.1 语音输入激活与触发
- **按住说话模式**：
  - 用户按下修饰键（默认双击或长按 `Option` 键）即刻激活录音，屏幕中心弹出半透明悬浮窗（Bubble Overlay）。
  - 录音期间保持悬浮显示，松开 `Option` 键立即结束录音，并在当前光标处自动模拟打字输入文字。
- **单击开始/停止模式**：
  - 允许在偏好设置中切换为单击触发录音、再次单击停止录音的交互形式。
- **自适应悬浮 UI**：
  - 采用纯 SwiftUI 渲染的高保真自适应渐变半透明玻璃卡片背景（融合系统 Accent Color 液态流光与 windowBackgroundColor 自适应），配合外边缘 1.5pt 的左上角强反光折射描边、3D 线性反射亮面，以及双重环境悬浮投影。
  - 悬浮窗宽度随流式字符的长度自适应向右铺展。

### 2.2 多 ASR 引擎管理与按需下载
- **引擎切换与 Locale 控制**：用户可在偏好设置中切换不同的 ASR 引擎，并可动态指定识别 Locale（自动、中文、英文），当处于“自动”模式时使用 SenseVoice-Small 识别多语言，或动态改变原生 ASR 的 locale。
- **模型按需下载**：首次选择本地 ONNX 模型时，程序启动断点下载（支持代理镜像加速如 `ghfast.top`），下载后自动用系统的 `tar` 解包验证。

### 2.3 开机默认自启动 (Launch at Login)
- **开机自启动开关**：偏好设置提供一个“开机自动启动”选项。
- **开机自启动规则**：
  - **默认关闭**：第一次下载并打开本应用时，此功能默认不开启。
  - **自适应路径绑定**：开启后，自动捕获当前应用所在的绝对路径并注册到系统后台中。无论是放在“应用程序”还是任意自定义文件夹，均可确保开机后正常拉起应用。

### 2.4 ASR 应用层智能纠偏词典与语气词过滤
- **智能纠偏映射**：
  - **内置默认规则**：系统预设一套常见专业名词 of 纠偏规则，如将常误识别的 `openclaw` / `open claw` 自动替换为 `open cloud`，`her agent` 自动替换为 `Hermes Agent`，`clod code` 替换为 `Claude Code`，以及规范 `GitHub`、`ChatGPT`、`git commit` 等词。
  - **自定义纠错规则**：允许用户在偏好设置中自定义 `识别错的词 -> 正确的词`，每行一条规则，规则间通过 `->` 或 `:` 映射，系统不区分大小写地在最终打字前进行纠正。
- **口语语气词过滤**：
  - 偏好设置中提供“过滤多余语气词 (如：呃、啊、嗯、那个等)”开关。
  - 自动剥离句首、句中及句末的口语语气词并收紧标点，当整句仅有一个语气词本身时则不予过滤，防止内容被误清空。
- **词典导入与导出**：
  - 支持导入外部纠错 `.txt` 词典并追加到现有规则中。
  - 支持导出当前纠错词典备份。
  - 支持导出原始听写历史日志，便于发给 AI 分析总结并转化为您的纠偏规则。

### 2.5 高清自适应状态栏图标
- **高清渲染**：托盘状态栏图标适配高清物理像素，防止低分辨率拉伸变形和边缘锯齿。
- **自适应系统主题**：图标使用 template 模式，能随 macOS 系统的深色、浅色模式（Dark/Light Mode）的变化以及背景透明度自动变换前景色。

---

## 3. 技术规格与关键调试记录

### 3.1 C-API 桥接与动态链接库 (libsherpa-onnx)
- 桥接头文件（`VoiceFlow-Bridging-Header.h`）对 `libsherpa-onnx-c-api` 进行直接绑定。
- **动态装载与代码签名修复**：通过 `install_name_tool` 对 dylib 路径修正，并使用 `codesign` 递归执行本地 Ad-hoc 代码签名，以解决 App 独立运行包的闪退问题。

### 3.2 苹果原生 ASR 重启及安全网防线
- **防止取消级联风暴**：在启动新识别任务前，先在主线程切断引用（`self.nativeTask = nil`），再调用 `oldTask?.cancel()`，阻断级联取消导致的死循环。
- **时间窗与安全网**：设定重启时间窗保护（必须间隔 `1.2秒` 以上）与文本长度安全网，防止空回调重写抹去已有的识别文字。

### 3.3 AppKit 独立生命周期与多窗口冲突修复
- **单窗口与静默托盘启动**：摒弃 SwiftUI `@main` 接管的默认生成空白窗口行为，使用纯 AppKit 的 `main.swift` 与 `AppDelegate`，以 `LSUIElement=1` 静默菜单托盘模式启动。
- **偏好设置窗口尺寸固定**：窗口大小设为 `420x580`，内部组件使用 `ScrollView` 包裹，防折叠溢出。

### 3.4 LaunchAgent 机制实现开机自启动
- **兼容性考量**：废弃了命令行下容易编译报错的 `ServiceManagement` 框架 `SMAppService` API，改用更加强壮通用的 **`LaunchAgents`** 机制。
- **状态读取与写入**：
  - 启动状态绑定在 `SpeechRecognizer.launchAtLogin` 属性。
  - 当开机自启开启时，应用会在用户目录 `~/Library/LaunchAgents/` 下写入一个 `com.hujia.VoiceFlow.plist` 文件，内含应用的当前绝对路径（`Bundle.main.bundlePath`）与 `RunAtLoad` 参数。
  - 首次打开应用时，因为 `plist` 文件不存在，该开关在界面中呈现为关闭状态。当取消勾选时，文件会被安全删除。

### 3.5 应用层后处理智能纠偏与语气词过滤实现
- **拦截流程**：
  ```
  [语音输入录制结束] -> [ASR 模型识别原始文本] 
        |
        +-> [记录原始文本到 dictation_history.txt]
        |
        +-> [应用默认及自定义纠偏规则，执行不区分大小写的替换]
        |
        +-> [正则表达式匹配，去除多余口语语气词并收紧逗句标点]
        |
        V
  [打字机模拟按键，最终文本上屏输出]
  ```
- **正则语气词过滤规则 (removeFillerWords)**：
  - 匹配句中夹带的语气词并收紧标点：`[,，\s]+(嗯|啊|呀|呃|哦|呢|吧|哈)+[,，\s]+` $\rightarrow$ `，`
  - 剔除句末语气词保留标点：`(嗯|啊|呀|呃|哦|呢|吧|哈)+[.。!！?？]+` $\rightarrow$ `。`
  - 剥离句首语气词：`^\s*(嗯|啊|呀|呃|哦|呢|吧|哈)+[,，\s]*` $\rightarrow$ `""`
  - 字与字之间的单音语气词直接干掉：`(?<=.)(嗯|啊|呀|呃|哦|呢|吧|哈)(?=.)` $\rightarrow$ `""`
  - 句首或整句为语气词的防空保护：过滤前检测，若整句仅为单音或极短语气词则不予过滤，防止返回空串。

### 3.6 托盘图标的高清自适应技术规格
- **资源配置**：放弃原先拉伸 18x18 导致模糊和扭曲的做法。
- **高清方案**：托盘使用 22x22 尺寸的高清晰度 PDF 矢量或点阵图标，在初始化 `NSStatusBar` 按钮时：
  ```swift
  if let button = statusItem.button {
      let image = NSImage(named: "StatusIcon")
      image?.size = NSSize(width: 22, height: 22)
      image?.isTemplate = true // 声明为系统模板图标
      button.image = image
  }
  ```
  由此保证无论系统菜单栏是什么背景（浅色、深色、透明高斯模糊），图标都能以最饱满、最精准的前景色显示。

---

## 4. 里程碑与后续版本规划

### 4.1 里程碑（已于 2026-06 交付）
- [x] 多 ASR 引擎一键切换与自动下载（SenseVoice/Paraformer/Zipformer/Native）。
- [x] 混合流式听写架构（苹果原生 ASR 实时预览 + 本地 ASR 结束后覆盖上屏）。
- [x] 基于 LaunchAgent 的开机自启动选项（默认关闭，支持动态路径）。
- [x] ASR 应用层智能纠偏词典（支持错误词对照替换）与语气词过滤机制。
- [x] 纠错词库的导入/导出，听写历史日志导出，便于 AI 持续优化词典。
- [x] 托盘图标 22x22 高清化与 template 深浅色自适应渲染。

### 4.2 后续版本规划 (Roadmap)
- [ ] 支持按应用类别绑定独立的快捷词典（例如：在 Terminal 下自动开启特定编程术语映射，而在 Pages 下使用日常书写字典）。
- [ ] 结合大语言模型本地运行引擎（如 Ollama / llama.cpp），在本地后台异步进行句法润色与智能修辞替换。
